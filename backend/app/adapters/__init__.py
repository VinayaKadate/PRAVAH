# Adapter pattern for external service integrations.
# Each adapter has a base abstract class and a mock implementation.
# Later phases swap mocks for real integrations without touching calling code.
